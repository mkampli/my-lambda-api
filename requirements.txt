# Python dependencies for the Lambda function
# This function only uses the Python standard library (json, datetime)
# so there are no third-party packages to install.
#
# If you add packages like 'requests' or 'boto3' later, add them here:
# requests==2.31.0
# boto3==1.34.0   (pre-installed on Lambda, but pin for reproducibility)
